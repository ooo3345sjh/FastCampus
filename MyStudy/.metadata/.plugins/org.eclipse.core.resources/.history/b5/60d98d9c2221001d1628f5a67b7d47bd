package ch12;

/*
 * 날짜 : 2022-08-21
 * 이름 : 서정현
 * 내용 : 중복되지 않게 자료를 관리하는 Set 인터페이스를 구현한 클래스와 그 활용
 */

import java.util.*;

public class MemberHashSet {
	HashSet<Member1> memberSet;
		
	public MemberHashSet() {
		memberSet = new HashSet<>();
	}
	
	public MemberHashSet(int size) {
		memberSet = new HashSet<>(size);
	}
	
	public void addMember(Member1 member) {
		memberSet.add(member);
	}
	
	public boolean removeMember(Member1 member) {
		/*
		for(int i=0; i<memberArray.size(); i++) {
			
			int memberId = memberArray.get(i).getMemberId();
			if(memberId == member.getMemberId()) {
				memberArray.remove(i);
				return true;
			}
		}
		*/
		
		Iterator<Member1> i = memberSet.iterator();
		
		while(i.hasNext()) {
			Member1 temp = i.next();
			if(temp.getMemberId() == member.getMemberId()) {
				memberSet.remove(member);
				return true;
			}
		}
		
		System.out.println(member.getMemberId() + "가 존재하지 않습니다.");
		return false;
	}
	
	public void showAllMember() {
		for(Member1 member : memberSet) {
			System.out.println(member);
		}
		System.out.println();
	}


}
